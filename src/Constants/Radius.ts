// MemeGenerator Border Radius System
// Based on style_design.md

const Radius = {
  none: 0,     // Sharp corners (inputs, some cards)
  sm: 4,       // Subtle rounding (buttons, chips)
  md: 8,       // Medium rounding (cards, inputs)
  lg: 12,      // Large rounding (images, featured cards)
  xl: 16,      // Extra large (modal corners, special cards)
  round: 999,  // Fully rounded (pills, circular avatars)
}

export default Radius
